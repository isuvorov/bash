# Функция cd с поддержкой русской раскладки
# св юю → cd ..
# св ю → cd .
св() {
    local path="$1"
    # Транслитерация русских символов в английские (нижний ряд клавиатуры)
    path="${path//ю/.}"
    path="${path//юю/..}"
    cd "$path"
}
